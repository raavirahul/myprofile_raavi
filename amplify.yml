version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - corepack enable || true
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: /
